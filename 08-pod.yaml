apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  labels:
    project: roboshop
spec:
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command: ["sleep","2000"]
  serviceAccountName: roboshop-secret-reader